import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    gap: 20,
    padding: 20,
  },
  completedContainer: {
    gap: 10,
    flexDirection: 'row',
    alignItems: 'center',
  },
  button: {
    width: 40,
  },
  header: {
    fontSize: 18,
    marginBottom: 8,
    fontWeight: '700',
  },
  value: {
    fontSize: 18,
  },
});

export default styles;
